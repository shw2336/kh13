<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="emp">
	<!-- resultType은 조회 결과의 형태를 의미하며 select에만 작성 -->
	<select id="list" resultType="EmpDto">
		select * from emp
	</select>	
	
	<insert id="add">
		insert into emp (emp_no, emp_name, emp_dept, emp_date, emp_sal
		) 
		values ( #{empNo} , #{empName} , #{empDept}, #{empDate}, #{empSal} )
	</insert>
	
	<update id="edit">
		update emp set emp_name=#{empName}, emp_dept=#{empDept}, 
		emp_date=#{empDate}, emp_sal=#{empSal}  
		where emp_no=#{empNo}
	</update>
	
	<delete id="remove">
		delete emp where emp_no = #{empNo}
	</delete>
	
	<!-- 
	상세(단일)조회
	- 구문은 목록때와 동일하게 생성
	- 차이는 호출할 때 selectList로 부르냐, selectOne으로 부르냐의 차이
	 -->
	 <select id="find" resultType="EmpDto">
	 	select * from emp where emp_no = #{empNo}
	 </select>
	
</mapper>